<Window x:Class="ARQUEO.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ARQUEO"
        mc:Ignorable="d"
        Title="Window1" Height="310" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Background="CornflowerBlue">        
        <StackPanel Grid.ColumnSpan="2">
            <StackPanel.Background>
                <LinearGradientBrush EndPoint="0.7,2" StartPoint="0.0,0">
                    <GradientStop Color="RoyalBlue"/>
                    <GradientStop Color="Magenta" Offset="0.8"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <TextBlock Text="BIENVENIDO" FontSize="40" VerticalAlignment="Center" HorizontalAlignment="Center" Background="Transparent" Foreground="White" Margin="0,10,0,25" FontWeight="UltraLight" ></TextBlock>
            <TextBlock Text="ENCARGADO ARQUEO:" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Center" Background="Transparent" Foreground="White" FontWeight="Thin" Margin="-120,0,0,-6"></TextBlock>
            <TextBox x:Name="NombreArqueo" Width="250" Height="23" FontSize="13" FontWeight="SemiBold" Foreground="DarkSlateGray" Background="GhostWhite" MaxLength="15" TextAlignment="Center" Margin="10" Padding="2" ></TextBox>
            <TextBlock Text="IDENTIFICACIÓN:" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Center" Background="Transparent" Foreground="White"  FontWeight="Thin" Margin="-155,0,0,-6"></TextBlock>
            <TextBox x:Name="IDarqueador" Width="250" Height="23" FontSize="13" FontWeight="SemiBold" Foreground="DarkSlateGray" Background="GhostWhite" MaxLength="15" TextAlignment="Center" Margin="10" Padding="2"></TextBox>
            <Button Background="White" Content="LOGIN" 
                    Foreground="Black" FontSize="18" FontWeight="ExtraLight"
                    Width="125" Height="40" VerticalAlignment="Center" HorizontalContentAlignment="Center" 
                    VerticalContentAlignment="Center" Margin="0,10,0,0"
                    BorderThickness="2" BorderBrush="White"
                    Click="ButtonLogin" >
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="15"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.FontSize="{TemplateBinding FontSize}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              TextElement.FontWeight="{TemplateBinding FontWeight}"/>
                        </Border>                        
                    </ControlTemplate>
                </Button.Template>
              
            </Button>
            



        </StackPanel>


    </Grid>
</Window>
